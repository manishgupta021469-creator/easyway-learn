Easyway Learn V62

Fix: Create Subject / Create Book typing reliability.

The create Subject and create Book actions now use the browser's native prompt input instead of the custom modal input. This bypasses mobile speech-recognition/render/focus conflicts that could prevent keyboard text entry. Subject and Book records are saved normally to the existing database structure, with audit entries and normal toast/render flow.

OCR changes from V61 are retained.
