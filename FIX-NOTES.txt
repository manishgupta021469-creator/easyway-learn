Easyway Learn V66 – OCR final deployment fix

1. OCR is now explicitly session-independent in the browser: page OCR is attempted even when the Student session token is missing or expired.
2. The server exposes /api/ocr and /api/ocr-batch before the authenticated student routes.
3. Added GET /api/ocr-status to verify Tesseract availability and Hindi/English language packs.
4. OCR errors are shown verbatim instead of being replaced with a misleading authentication message.
5. Normal page saving, assets, student state and assessments remain authenticated.
6. English + Hindi OCR, chapter detection and paragraph extraction remain enabled.

Deploy app.js and server.js together from the repository root. Commit both in one commit and wait for Render to show Live.
