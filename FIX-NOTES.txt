Easyway Learn V65 — OCR Authentication + PDF Preview Final Fix

1. OCR AUTHENTICATION FIX
   - /api/ocr and /api/ocr-batch are now stateless document-processing endpoints and no longer fail with “Authentication required” when a browser has an expired/stale learning-session token.
   - Student data APIs and page-asset saving remain authenticated.
   - Existing per-IP request rate limiting remains active.

2. OCR
   - Hindi + English OCR remains supported through Tesseract.
   - PDF pages are processed page-by-page with Poppler + Tesseract.
   - Chapter detection and paragraph extraction remain enabled.

3. PDF PAGE PREVIEW
   - CSP now explicitly permits blob: PDF iframe previews, fixing “This content is blocked” for locally stored PDF pages.

4. VALIDATION
   - app.js/server.js syntax checked.
   - Unauthenticated POST /api/ocr tested locally: HTTP 200, OCR text returned, chapter heading detected, paragraph returned.
   - ZIP integrity checked.
