Easyway Learn v58 - Modal/Input + cache fix

Fixes:
- Book/Subject/Chapter/Paragraph modal Close/Cancel buttons now use delegated event handling and work even if inline event attributes are blocked.
- Closing a modal stops speech recognition and removes stale modal overlays.
- Focusing a modal input/textarea/select stops any active recognition so typing is never interfered with by microphone recovery.
- Added sw.js v58 with network-first loading for app.js/index/styles/sw to prevent stale service-worker JavaScript from hiding fixes after Render deploys.

Replace at repository root:
- app.js
- server.js
- Dockerfile
- sw.js
Commit once: Fix modal buttons, book typing and service worker cache
